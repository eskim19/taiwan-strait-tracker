__pycache__/
*.py[cod]
.venv/
venv/
# 조건부 GET 캐시. 러너는 매번 새로 뜨므로 커밋해도 이득이 없고,
# 매 실행마다 내용이 바뀌어 무의미한 커밋만 늘린다.
state/
.DS_Store

# 원자적 쓰기 임시파일과 --rebuild 백업이 docs/data 에 생기는데,
# 워크플로가 git add docs/data 를 하므로 명시적으로 제외한다.
*.bak
*.tmp
